steps:
  - label: "test"
    plugins:
      - docker-compose#v3.0.3:
          run: test
  - label: "rust doc"
    plugins:
      - docker-compose#v3.0.3:
          run: doc
  - label: "check readme"
    plugins:
       - docker-compose#v3.0.3:
          run: check-readme